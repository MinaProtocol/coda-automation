{- Constant values used across configuration should be collected here. If a
   value can be changed per config (i.e. it's read as an ENV var), it should
   not live here. -}

{
  codaConfigPath = "/root/.coda-config"
}
